export default class Admin {
  constructor(adminId, authUserId, fullName, email) {
    this.adminId = adminId;
    this.authUserId = authUserId;
    this.fullName = fullName;
    this.email = email;
  }
}